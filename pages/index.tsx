import { Box, Center } from '@mantine/core';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Center sx={{ minHeight: '100vh' }}>
      <div>Test</div>
    </Center>
  );
};

export default Home;
